-- nvim-treesitter-context: Shows the current function/class context at the top of the window
-- when the actual declaration has scrolled out of view. Useful for navigating large files.
-- Commands: :TSContextToggle, :TSContextEnable, :TSContextDisable

-- return {
--   'nvim-treesitter/nvim-treesitter-context',
--   config = function()
--     require('treesitter-context').setup({
--       enable = true,
--     })
--   end
-- }
return {}
